<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link tal:repeat="url request.webassets_env['default-css'].urls()" href="${url}" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <div class="container">
      <tal:location condition="location">
        <p style="font-size:10pt">Send To:<br />
          <address>
            <strong>${sent_name}</strong>
            <span tal:omit-tag="" tal:define="value location.long_title1" tal:condition="value">${value}</span><br />
            <span tal:omit-tag="" tal:define="value location.long_title2" tal:condition="value">${value}</span><br />
            <span tal:omit-tag="" tal:define="value location.address_street" tal:condition="value">${value}</span><br />
            <span tal:omit-tag="" tal:define="value location.address_city" tal:condition="value">${value}</span>,
            <span tal:omit-tag="" tal:define="value location.address_state" tal:condition="value">${value}</span>
            <span tal:omit-tag="" tal:define="value location.address_zip" tal:condition="value">${value}</span><br/>
            <span tal:omit-tag="" tal:define="value location.phone_number" tal:condition="value">Phone: ${value}</span><br/>
            <span tal:omit-tag="" tal:define="value location.fax_number" tal:condition="value">Fax: ${value}</span><br/>
          </address>
        </p>
      </tal:location>
      <tal:location condition="not:location">
        <p>Multiple send locations were found. Please update your aliquot, or change your filter.</p>
      </tal:location>
      <table class="table table-bordered table-condensed table-striped">
        <tr>
          <th width="10%">Aliquot ID</th>
          <th width="10%">Patient</th>
          <th width="18%">Study / Week</th>
          <th width="14%">Store Date</th>
          <th width="14%">Type</th>
          <th width="14%">Volume / # of Cells</th>
          <th width="20%">Notes</th>
        </tr>
        <tr tal:repeat="sample full_query">
          <td><code>${sample.id}</code></td>
          <td><code>${sample.specimen.patient.pid}</code></td>
          <td>${sample.specimen.cycle.study.short_title} - ${sample.specimen.cycle.week}</td>
          <td><code>${sample.store_date}</code></td>
          <td>${sample.aliquot_type.title}</td>
          <td>${(sample.volume > 0 and sample.volume) or (sample.cell_amount > 0 and sample.cell_amount) or '--'}</td>
          <td>
            <p tal:condition="sample.notes">${sample.notes}</p>
            <p tal:condition="sample.sent_notes">${sample.sent_notes}</p>
          </td>
        </tr>
      </table>
    </div>
  </body>
</html>
