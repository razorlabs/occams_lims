<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      metal:use-macro="context/main_template/macros/master"
      i18n:domain="hive.lab"
      xml:lang="en"
      lang="en">
  <metal:content-core fill-slot="content-core">
    <metal:content-core define-macro="content-core">
        <tal:data define="form view/getQueueForm">
            <tal:hasdata tal:condition="form">
                <p>Use this page to add data to all of the aliquot pending checkout, and complete checkout.</p>
                <tal:crud replace="structure form" />
                <a href="#" name="" style="display:none"></a>
                <br class="visualClear" />
                <ul>
                    <li>
                        <b>Print List</b>: Print a list to take to storage.
                    </li>
                    <li>
                        <b>Check Out</b>: Place the selected aliquot in the fnial stage for check out (below).
                    </li>
                    <li>
                        <b>Release from Queue</b>:Release the hold on the selected aliquot, returning them to a checked in state.
                    </li>
                    <li>
                     <b><a href="./edit-aliquot">Mark Inaccurate</a></b>: Mark the selected aliquot as inaccurate. Edit them on the <a href="./edit-aliquot">edit aliquot screen</a>.
                    </li>
                    <li>
                        <b>Mark Missing</b>: Mark the selected aliquot as missing.
                    </li>
                </ul>
            <hr class="visualClear" />
            </tal:hasdata>
        </tal:data>
        <tal:data define="form view/getCrudForm">
            <tal:hasdata tal:condition="form">
                <p>Use this page to add data to all of the aliquot pending checkout, and complete checkout.</p>
                <tal:crud replace="structure form" />
                <a href="#" name="" style="display:none"></a>
                <br class="visualClear" />
                <ul>
                    <li>
                        <b>Print List</b>: Print a list to take to storage.
                    </li>
                    <li>
                        <b>Check Out</b>: Place the selected aliquot in the fnial stage for check out (below).
                    </li>
                    <li>
                        <b>Release from Queue</b>:Release the hold on the selected aliquot, returning them to a checked in state.
                    </li>
                    <li>
                     <b><a href="./edit-aliquot">Mark Inaccurate</a></b>: Mark the selected aliquot as inaccurate. Edit them on the <a href="./edit-aliquot">edit aliquot screen</a>.
                    </li>
                    <li>
                        <b>Mark Missing</b>: Mark the selected aliquot as missing.
                    </li>
                </ul>
            <hr class="visualClear" />
            </tal:hasdata>
        </tal:data>
      </metal:content-core>
  </metal:content-core>
</html>