<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      metal:use-macro="context/main_template/macros/master"
      i18n:domain="hive.lab"
      xml:lang="en"
      lang="en">
  <metal:content-core fill-slot="content-core">
    <metal:content-core define-macro="content-core">
        <tal:data define="form view/filter">
            <tal:hasdata tal:condition="form">
                <p>
                Enter the criteria you would like to use as a filter the checked out aliquot. The filter will persist while you are logged in. Be sure to remove the filter to make sure you are seeing all available aliquot if that is your wish.
                </p>
                <tal:crud replace="structure form" />
                <a href="#" name="" style="display:none"></a>
            </tal:hasdata>
            <br class="visualClear" />
        </tal:data>
        <tal:data define="form view/crudform">
            <tal:hasdata tal:condition="form">
                <p>Checked out aliquot.</p>
                <tal:crud replace="structure form" />
                <a href="#" name="" style="display:none"></a>
                <br class="visualClear" />
                    <li>
                        <b>Save Changes</b>: Save changes to all aliquot.
                    </li>
                    <li>
                        <b>Check In</b>:Release the hold on the selected aliquot, returning them to a checked in state.
                    </li>
            </tal:hasdata>
            <tal:nodata tal:condition="not:form">
                <p>
                No Aliquot match your filter.</p>
            </tal:nodata>
        </tal:data>
    </metal:content-core>
  </metal:content-core>
</html>
    
